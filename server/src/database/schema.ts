import { index, integer, real, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
const id = () => text('id').primaryKey();
const utc = (name: string) => text(name).notNull();
export const users = sqliteTable('users',{id:id(),openid:text('openid').unique(),nickname:text('nickname').notNull().default(''),avatarUrl:text('avatar_url'),status:text('status').notNull().default('active'),createdAt:utc('created_at'),updatedAt:utc('updated_at')});
export const userSessions = sqliteTable('user_sessions',{id:id(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),tokenHash:text('token_hash').notNull().unique(),expiresAt:utc('expires_at'),revokedAt:text('revoked_at'),createdAt:utc('created_at')},t=>[index('sessions_user_idx').on(t.userId)]);
export const addresses = sqliteTable('addresses',{id:id(),userId:text('user_id').notNull().references(()=>users.id),contactName:text('contact_name').notNull(),phone:text('phone').notNull(),province:text('province').notNull().default(''),city:text('city').notNull().default(''),district:text('district').notNull().default(''),poiName:text('poi_name').notNull().default(''),formattedAddress:text('formatted_address').notNull(),detailAddress:text('detail_address').notNull().default(''),longitude:real('longitude').notNull(),latitude:real('latitude').notNull(),usageType:text('usage_type').notNull(),isDefaultSender:integer('is_default_sender',{mode:'boolean'}).notNull().default(false),isDefaultReceiver:integer('is_default_receiver',{mode:'boolean'}).notNull().default(false),createdAt:utc('created_at'),updatedAt:utc('updated_at'),deletedAt:text('deleted_at'),migrationKey:text('migration_key')},t=>[index('addresses_user_idx').on(t.userId),uniqueIndex('addresses_user_migration_uq').on(t.userId,t.migrationKey)]);
export const adminUsers = sqliteTable('admin_users',{id:id(),username:text('username').notNull().unique(),passwordHash:text('password_hash').notNull(),role:text('role').notNull().default('reviewer'),status:text('status').notNull().default('active'),createdAt:utc('created_at'),updatedAt:utc('updated_at')});
export const adminSessions = sqliteTable('admin_sessions',{id:id(),adminUserId:text('admin_user_id').notNull().references(()=>adminUsers.id,{onDelete:'cascade'}),tokenHash:text('token_hash').notNull().unique(),expiresAt:utc('expires_at'),revokedAt:text('revoked_at'),createdAt:utc('created_at')});
export const adminWechatBindings = sqliteTable('admin_wechat_bindings',{id:id(),adminUserId:text('admin_user_id').notNull().references(()=>adminUsers.id,{onDelete:'cascade'}),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),grantedBy:text('granted_by').notNull().references(()=>adminUsers.id),createdAt:utc('created_at'),updatedAt:utc('updated_at'),revokedAt:text('revoked_at')},t=>[uniqueIndex('admin_wechat_admin_uq').on(t.adminUserId),uniqueIndex('admin_wechat_user_uq').on(t.userId)]);
export const orders = sqliteTable('orders',{id:id(),orderNo:text('order_no').notNull().unique(),userId:text('user_id').notNull().references(()=>users.id),vehicleId:text('vehicle_id').notNull(),status:text('status').notNull(),pickupType:text('pickup_type').notNull(),scheduledAt:text('scheduled_at'),scheduledEndAt:text('scheduled_end_at'),customerRemark:text('customer_remark'),reviewedBy:text('reviewed_by').references(()=>adminUsers.id),reviewedAt:text('reviewed_at'),rejectionReason:text('rejection_reason'),internalNote:text('internal_note'),userNote:text('user_note'),idempotencyKey:text('idempotency_key').notNull(),createdAt:utc('created_at'),updatedAt:utc('updated_at')},t=>[uniqueIndex('orders_user_idempotency_uq').on(t.userId,t.idempotencyKey),index('orders_user_idx').on(t.userId)]);
export const orderAddresses = sqliteTable('order_addresses',{id:id(),orderId:text('order_id').notNull().references(()=>orders.id,{onDelete:'cascade'}),role:text('role').notNull(),contactName:text('contact_name').notNull(),phone:text('phone').notNull(),province:text('province').notNull(),city:text('city').notNull(),district:text('district').notNull(),poiName:text('poi_name').notNull(),formattedAddress:text('formatted_address').notNull(),detailAddress:text('detail_address').notNull(),longitude:real('longitude').notNull(),latitude:real('latitude').notNull()},t=>[uniqueIndex('order_address_role_uq').on(t.orderId,t.role)]);
export const orderItems = sqliteTable('order_items',{id:id(),orderId:text('order_id').notNull().references(()=>orders.id,{onDelete:'cascade'}),category:text('category').notNull(),name:text('name').notNull(),quantity:integer('quantity').notNull(),estimatedWeightKg:real('estimated_weight_kg').notNull(),lengthMm:integer('length_mm'),widthMm:integer('width_mm'),heightMm:integer('height_mm'),fragile:integer('fragile',{mode:'boolean'}).notNull().default(false),oversized:integer('oversized',{mode:'boolean'}).notNull().default(false),needCarry:integer('need_carry',{mode:'boolean'}).notNull().default(false),remark:text('remark')});
export const orderQuotes = sqliteTable('order_quotes',{id:id(),orderId:text('order_id').notNull().references(()=>orders.id),baseFeeCents:integer('base_fee_cents').notNull(),distanceFeeCents:integer('distance_fee_cents').notNull(),vehicleFeeCents:integer('vehicle_fee_cents').notNull(),serviceFeeCents:integer('service_fee_cents').notNull(),discountCents:integer('discount_cents').notNull(),totalCents:integer('total_cents').notNull(),distanceMeters:integer('distance_meters').notNull(),expiresAt:utc('expires_at'),createdBy:text('created_by').notNull().references(()=>adminUsers.id),createdAt:utc('created_at')},t=>[index('quotes_order_idx').on(t.orderId)]);
export const orderStatusLogs = sqliteTable('order_status_logs',{id:id(),orderId:text('order_id').notNull().references(()=>orders.id),fromStatus:text('from_status'),toStatus:text('to_status').notNull(),operatorType:text('operator_type').notNull(),operatorId:text('operator_id').notNull(),remark:text('remark'),createdAt:utc('created_at')});
export const payments = sqliteTable('payments',{id:id(),orderId:text('order_id').notNull().references(()=>orders.id),amountCents:integer('amount_cents').notNull(),provider:text('provider').notNull(),status:text('status').notNull(),providerTradeNo:text('provider_trade_no'),createdAt:utc('created_at'),updatedAt:utc('updated_at')});
export const auditLogs = sqliteTable('audit_logs',{id:id(),adminUserId:text('admin_user_id').notNull().references(()=>adminUsers.id),action:text('action').notNull(),resourceType:text('resource_type').notNull(),resourceId:text('resource_id').notNull(),detailJson:text('detail_json').notNull(),createdAt:utc('created_at')});
export const vehicleCatalog = sqliteTable('vehicle_catalog',{id:id(),name:text('name').notNull(),fullName:text('full_name').notNull(),subtitle:text('subtitle').notNull(),description:text('description').notNull(),specsJson:text('specs_json').notNull(),scenesJson:text('scenes_json').notNull(),restrictionsJson:text('restrictions_json').notNull(),modesJson:text('modes_json').notNull(),pricingHintJson:text('pricing_hint_json').notNull(),tagsJson:text('tags_json').notNull(),enabled:integer('enabled',{mode:'boolean'}).notNull().default(true),requiresApproval:integer('requires_approval',{mode:'boolean'}).notNull().default(false),sortOrder:integer('sort_order').notNull().default(0),createdAt:utc('created_at'),updatedAt:utc('updated_at')});
export const vehicleImages = sqliteTable('vehicle_images',{id:id(),vehicleId:text('vehicle_id').notNull().references(()=>vehicleCatalog.id,{onDelete:'cascade'}),url:text('url').notNull(),objectKey:text('object_key').notNull(),isPrimary:integer('is_primary',{mode:'boolean'}).notNull().default(false),sortOrder:integer('sort_order').notNull().default(0),createdAt:utc('created_at')},t=>[index('vehicle_images_vehicle_idx').on(t.vehicleId)]);
export const contentBanners = sqliteTable('content_banners',{id:id(),imageUrl:text('image_url').notNull(),objectKey:text('object_key').notNull(),title:text('title').notNull(),linkType:text('link_type').notNull(),linkTarget:text('link_target').notNull(),sortOrder:integer('sort_order').notNull().default(0),enabled:integer('enabled',{mode:'boolean'}).notNull().default(true),createdAt:utc('created_at'),updatedAt:utc('updated_at')});
export const pricingRuleVersions = sqliteTable('pricing_rule_versions',{id:id(),version:integer('version').notNull().unique(),status:text('status').notNull(),configJson:text('config_json').notNull(),createdBy:text('created_by').notNull().references(()=>adminUsers.id),publishedBy:text('published_by').references(()=>adminUsers.id),createdAt:utc('created_at'),updatedAt:utc('updated_at'),publishedAt:text('published_at')});
